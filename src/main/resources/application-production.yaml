spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jwt:
    jwtSecret: ${JWT_SECRET}
    jwtExpiration: ${JWT_EXPIRATION}
  jpa:
    hibernate:
      ddl-auto: update